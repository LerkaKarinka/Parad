services:
  web:
    build: .
    container_name: parad-app
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/parad.db
    restart: unless-stopped
