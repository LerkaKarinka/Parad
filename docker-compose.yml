services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - db_data:/app/data
    environment:
      - FLASK_ENV=development
      - DB_PATH=/app/data/parad.db
    restart: unless-stopped

volumes:
  db_data:
