version: '3.8'

services:
  web:
    build: .
    container_name: parad-app
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data   # сохраняем SQLite базу на хост
    environment:
      - DB_PATH=/app/data/parad.db
    restart: unless-stopped
